[project]
name = "cdl2-rocrates"
version = "0.1.0"
description = "Research Data RO-Crate Tools (WP2)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "networkx>=3.5",
    "pandas>=2.3.3",
    "rocrate>=0.14.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "ruff>=0.13.3",
]
